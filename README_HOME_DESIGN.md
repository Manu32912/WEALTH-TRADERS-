# Wealth Traders homepage

This package uses the generated HD trading-desk landing visual as the homepage hero. The previous animated candlestick canvas is not used on the homepage; transparent link hotspots keep the hero navigation and calls-to-action functional, and a real HTML live-market ticker remains below the hero.

Keep the separate `backend/` directory already in the GitHub repository for Railway.
